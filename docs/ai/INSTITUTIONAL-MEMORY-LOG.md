# Institutional Memory Log

Historical reconstruction covers PR #001 through PR #034, including merged, closed-unmerged and open/draft work. Historical records are evidence, not automatic current authority.

Forward loop after the institutional-memory foundation:
PR event/merge → JARVIS routes knowledge review → assigned models inspect description/diff/tests/decisions → classify knowledge impact → update `PR-NNN.md`, current-state/ADR/system docs and `docs/agentic/` if needed → CI/review → merge → next task reads new baseline.