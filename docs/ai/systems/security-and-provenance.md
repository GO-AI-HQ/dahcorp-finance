# Security and Provenance

Credentials/tokens remain server-side and sensitive provider errors are sanitized. OAuth/token persistence is encrypted where implemented. Model inputs retain evidence/state provenance; asynchronous analysis must preserve what the model originally saw separately from later risk-check state.

A newer portfolio refresh cannot be mislabeled as model-time evidence.