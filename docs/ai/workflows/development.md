# Development Workflow

Read current institutional memory → define objective and authority ceiling → inspect code/PR lineage → implement narrowly → deterministic tests/build → verify no authority widening/fixture promotion → update docs/history → PR → review/merge.

Safety and financial semantics are acceptance criteria, not cleanup.