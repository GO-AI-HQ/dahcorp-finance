# Testing Workflow

Test pure financial arithmetic, account scope, broker authority, allowlists, reserves, max notional/exposure, stale/UNKNOWN evidence, provider fallback, preview expiry/reuse, confirmation, fresh revalidation, ambiguous submission, Shadow no-write behavior, model provenance and deterministic fallback semantics.

Reliability harness results must distinguish controlled injected-failure evidence from observed production SLA.