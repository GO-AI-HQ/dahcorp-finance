# Release Workflow

A release requires tests/build green, explicit production-data semantics, broker/account/allowlist authority unchanged unless intentionally reviewed, safe credentials/errors, fresh execution revalidation, no automatic ambiguous retries and current institutional memory.

Execution-mode widening is a separate release decision, never an incidental merge side effect.