# Incident Response

Classify provider outage, stale/corrupt snapshot, broker OAuth/execution issue, calculation defect, model-semantic error, timeout/performance regression or security incident. Preserve evidence and authority. Never fix an outage by introducing fake production data or widening execution.

Use last-known-good only within policy window; expired evidence becomes unusable. Ambiguous broker placement requires reconciliation, not retry.