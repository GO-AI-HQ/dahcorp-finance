# PR #015 — Expose exact OpenAI prompt variable errors

**Disposition:** Merged.

## Purpose
Added sanitized `error.param` capture for prompt-variable diagnosis.

## Institutional impact
Observability can expose bounded identifier/path-shaped error metadata while rejecting arbitrary sensitive provider text.

## Current relevance
Provider diagnostic provenance.