# PR #035 — Establish DAHCorp Finance institutional memory and agentic CIO operating system

**Disposition:** Open draft.

## Purpose
Create the canonical onboarding/operating corpus for DAHCorp Finance and reconstruct repository history from PR #001 through #034. Establish the enterprise JARVIS projection, shared agent schemas, explicit model roles, MCP contracts, authority/approval rules, observability and forward self-updating institutional-memory workflow.

## Institutional impact
This PR does not widen broker execution. It codifies the durable operating doctrine: OpenAI produces an independent portfolio strategy proposal; Claude independently challenges; Perplexity supplies sourced research; Grok is a bounded tactical challenger/future executor only when runtime policy explicitly allows it; Strategy Lab reconciles; deterministic risk, account mandates, broker state and fresh execution evidence authorize.

## Historical boundary
Merged runtime authority on `main` is through PR #032. PR #012, #033 and #034 remain open/unmerged and are recorded as such rather than promoted to current truth.

## Current relevance
This PR becomes the forward institutional-memory boundary. Every subsequent PR receives its own chronological `PR-NNN.md` and updates affected canonical docs.