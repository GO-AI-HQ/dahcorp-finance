# Finance Self-Improvement and PR Policy

Agent/model failures can trigger diagnose → branch → tests → PR → review → merge → institutional-memory update. Models cannot self-modify production prompts, risk rules, allowlists or execution flags outside audited version control.

Outcome calibration can improve recommendations, but never silently widen autonomy.