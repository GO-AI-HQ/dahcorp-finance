# MCP — PostgreSQL / Durable State

Expose scoped Portfolio/Market/Intelligence/Shadow/receipt state through approved interfaces. Models must not receive arbitrary production SQL write authority. Mutations require application services, identity/version checks and audit receipts.